function attention(s){return s?.unread?'<b class="attention-dot" title="Unread messages"></b>':''}
function renderProjects(){let chat='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15a4 4 0 0 1-4 4H8l-5 3V7a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4z"></path></svg>';$('projects').innerHTML=(data.projects||[]).map(p=>{let active=p.id===projectId,projectUnread=p.sessions.some(s=>s.unread),children='';if(active){let team=p.sessions.find(x=>x.kind==='team'),custom=p.sessions.filter(x=>x.kind==='custom'&&!x.archived);let roster=(data.seats||[]).map(s=>{let m=member(s.occupant),direct=p.sessions.find(x=>x.kind==='direct'&&x.participants[0]===s.id);return `<div class="roster-line ${direct?.id===sessionId?'active':''} ${direct?.unread?'unread':''}" data-open-session="${direct?.id||''}" role="button" tabindex="0" title="Open direct session with ${esc(m?.name||s.role)}"><i style="background:${m?.colour||'var(--dim2)'}"></i><span>${esc(s.role)} · ${esc(m?.name||'Unassigned')}</span>${attention(direct)}</div>`}).join('');let customs=custom.map(s=>`<div class="tree-session ${s.id===sessionId?'active':''} ${s.unread?'unread':''}" data-open-session="${s.id}">${chat}<strong>${esc(s.name)}</strong>${attention(s)}<small>${s.participants.length}</small></div>`).join('');children=`<div class="project-tree"><div class="tree-session ${team?.id===sessionId?'active':''} ${team?.unread?'unread':''}" data-open-session="${team?.id||''}">${chat}<strong>Main</strong>${attention(team)}<small>${(data.seats||[]).length}</small></div><div class="main-roster">${roster}</div>${customs?'<div class="tree-label">Group sessions</div>'+customs:''}</div>`}return `<div class="project-block"><div class="project ${active&&!sessionId?'active':''} ${projectUnread?'unread':''}" data-project="${p.id}"><span><i class="dot ${p.state}"></i>${esc(p.name)}</span>${projectUnread?attention({unread:1}):''}<small>${p.state}</small></div>${children}</div>`}).join('');document.querySelectorAll('[data-project]').forEach(n=>n.onclick=()=>{projectId=n.dataset.project;sessionId=null;selected.clear();refresh()});document.querySelectorAll('[data-open-session]').forEach(n=>{let open=()=>{if(!n.dataset.openSession)return;sessionId=n.dataset.openSession;selected.clear();refresh()};n.onclick=open;n.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open()}}});let p=project();$('projectTitle').textContent=p?.name||'Projects';$('projectState').textContent=p?`${p.state} · ${size(p.storage)}`:'';$('toggleProject').textContent=p?.state==='open'?'Close project':'Open project';$('toggleProject').disabled=!p;$('newSession').disabled=!p;$('newSessionSmall').disabled=!p;$('newSeat').disabled=!p}
function renderSeats(){$('seats').innerHTML=(data.seats||[]).map(s=>{let m=member(s.occupant);return `<article class="overview-seat ${selected.has(s.id)?'selected':''}" data-seat="${s.id}" title="Open direct session"><div class="seat-avatar" style="background:${m?.colour||'var(--seat)'}">${esc((m?.name||s.role||'?')[0])}</div><div class="overview-seat-copy"><strong>${esc(s.role)}</strong><span>${esc(m?.name||'Unassigned')}${m?.kind==='agent'&&m.engine?' · '+esc(m.engine):''}</span></div><button class="seat-edit" data-edit-seat="${s.id}" title="Edit seat" aria-label="Edit ${esc(s.role)}">•••</button></article>`}).join('')||'<div class="empty">This project has no seats yet.</div>';document.querySelectorAll('[data-seat]').forEach(n=>{n.onclick=e=>{if(e.target.closest('[data-edit-seat]'))return;let id=n.dataset.seat;if(e.metaKey||e.ctrlKey){selected.has(id)?selected.delete(id):selected.add(id);return renderSeats()}selected=new Set([id]);let direct=project()?.sessions.find(x=>x.kind==='direct'&&x.participants[0]===id);if(direct){sessionId=direct.id;refresh()}};n.oncontextmenu=e=>{e.preventDefault();if(!selected.has(n.dataset.seat))selected=new Set([n.dataset.seat]);renderSeats();createSession()}});document.querySelectorAll('[data-edit-seat]').forEach(n=>n.onclick=e=>{e.stopPropagation();editSeat(n.dataset.editSeat)})}
function renderSessions(){let custom=(project()?.sessions||[]).filter(x=>x.kind==='custom'&&!x.archived);$('sessions').innerHTML=custom.map(s=>`<button class="overview-session" data-overview-session="${s.id}">${esc(s.name)} · ${s.participants.length} seats</button>`).join('')||'<span class="section-note">No custom sessions.</span>';document.querySelectorAll('[data-overview-session]').forEach(n=>n.onclick=()=>{sessionId=n.dataset.overviewSession;selected.clear();refresh()})}
